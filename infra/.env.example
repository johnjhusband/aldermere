# Aldermere infra environment.
#
# Real .env lives on the VPS at /opt/aldermere/infra/.env — generated once,
# stored DPAPI-encrypted locally as service "aldermere_pg" in the credentials
# cache. Never committed.

POSTGRES_USER=aldermere
POSTGRES_PASSWORD=<32-char random>
POSTGRES_DB=aldermere_control
